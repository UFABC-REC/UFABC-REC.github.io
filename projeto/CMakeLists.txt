cmake_minimum_required(VERSION 2.8)
project( detectar_carros )
find_package( OpenCV REQUIRED )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( detectar_carros detectar_carros.cpp )
target_link_libraries( detectar_carros ${OpenCV_LIBS} )
