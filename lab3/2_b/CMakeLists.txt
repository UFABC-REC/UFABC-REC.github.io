cmake_minimum_required(VERSION 2.8)
project( detector_canny )
find_package( OpenCV REQUIRED )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( detector_canny detector_canny.cpp )
target_link_libraries( detector_canny ${OpenCV_LIBS} )
